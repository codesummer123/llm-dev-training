# Day7-验收评估报告

日期：2026-04-07

主题：LangGraph 入门第一性原理：State、Node、Edge 与条件路由

## 1. 最终结论

Day7 已通过，评价为：优秀。

最终验收结果：

- `python main.py`
- `python -m pytest tests/ -q`
- 运行目录：`code/Day7`
- 结果：主程序运行通过，测试结果 `5 passed`

这说明 Day7 已经完成了从 LangChain 向 LangGraph 的第一次平稳过渡，而且抓住了正确主线：

- 不是先追复杂 Agent
- 而是先吃透图编排最底层的状态流动机制

## 2. 做得好的点

### 2.1 关注点已经从“节点代码”转向“状态如何驱动路径”

这是 Day7 最重要的成果。

你已经开始明显理解：

- 真正把流程串起来的不是节点本身
- 而是共享 state 的更新和读取

这说明你没有把 LangGraph 学成“另一种写函数的方式”，而是开始理解它的底层抽象。

### 2.2 条件路由已经对齐到分析后的 state

本轮一个关键修正是：

- 不再直接看原始输入字段做路由
- 而是先经过 `analyze_request`
- 再根据分析后的 state 决定走向

这一步非常重要，因为它体现了 LangGraph 的核心思维：

`先产生共享事实，再让流程围绕共享事实流动。`

### 2.3 图结构、节点职责、输出观察三件事都做到了

这次 Day7 不是只把 graph 跑通了，而是同时完成了：

- node 职责划分
- route_log 可观察
- final_summary 可解释

这说明你已经在按照“可调试工作流”的思路写图，而不是只追求能跑。

### 2.4 问答里对 route function 的理解明显更成熟了

你已经能意识到：

- route function 太复杂，往往不是“多写点判断”能解决
- 更应该先回头看 state 设计和 node 拆分

这是非常好的工程判断力。

## 3. 本轮关键修正

### 3.1 修正了 route function 的决策依据

最初 route function 更偏向直接看原始 `user_level`。  
这会削弱 LangGraph 的核心训练目标。

后续你改成：

- 优先基于 `needs_foundation`
- 再基于 `track`

这个修正非常到位，也让 Day7 真正从“输入驱动”提升成了“状态驱动”。

### 3.2 统一了 `track` 语义

你把中间层的 `"intermediate"` 路径语义收敛成了 `"standard"`，这很好。

因为在 workflow 里：

- state 字段名
- route label
- node 语义

最好尽量保持一致，否则后面图一复杂就容易混乱。

## 4. 当前能力评估

结合 Day1 到 Day7，可以给出这样的阶段判断：

- `L1 认知`：通过
- `L2 使用`：通过
- `L3 改造`：稳定
- `L4 设计`：持续增强

原因：

- 你已经掌握了 LangChain 的主干能力
- 现在开始进入 LangGraph 的底层编排抽象
- 你不再只看“代码对不对”，而开始看“执行流是否对齐抽象”

这说明训练正在往非常正确的方向走。

## 5. 对后续训练的意义

Day7 是一个非常关键的转折点。

从这里开始，你后续学 LangGraph 的这些能力会更容易落地：

- persistence
- durable execution
- interrupt / resume
- subgraph
- workflow patterns

因为这些能力本质上都是建立在：

- state 结构稳定
- node 职责清晰
- route 显式可解释

这三个基础之上。

## 6. 仍可继续优化的点

### 6.1 当前计划内容仍偏演示型

这在 Day7 完全合理。  
但后面进入更复杂的图时，你可以继续加强：

- state 更新粒度
- 节点产出结构化程度
- route_log 的可诊断性

### 6.2 当前测试主要覆盖节点与路由，还没有覆盖完整图的端到端行为

这同样不影响 Day7 通过。  
后续如果继续工程化，可以逐步补：

- graph.invoke 的结果断言
- 不同输入路径的端到端测试
- route_log 顺序测试

## 7. 一句话总结

Day7 的真正成果，不是“写出了第一张图”，而是开始建立：

`把工作流看成“共享状态驱动的执行系统”，而不是一堆节点连线。`
